{{include('partials/header.twig')}}

<div class="song-presentation">
  <div class="song-zone">
    {% for _song in songs %}
        {{_song.nb}}
        {{_song.title}}
        {{_song.time}}
    {% endfor %}
  </div>
</div>

</body>
</html>
